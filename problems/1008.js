export function problem (lines) {
    var num = lines[0];
    var hora = lines[1];
    var preco = lines[2];

    var sal = (hora * preco)

    console.log(`NUMBER = ${num}`)
    console.log(`SALARY = U$ ${sal.toFixed(2)}`)
 }
 